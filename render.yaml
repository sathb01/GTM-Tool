services:
  - type: web
    name: gtm-tool
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: gtm-data
      mountPath: /opt/render/project/src/server/data
      sizeGB: 1
