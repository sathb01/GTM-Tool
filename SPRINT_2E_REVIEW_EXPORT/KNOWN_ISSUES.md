# Known Issues

- Browser print headers/footers are controlled by the browser print dialog. The app CSS can reduce page artifacts, but users may still need to disable `Headers and footers` when saving PDF.
- Screenshots/PDF export were not captured in Codex because the in-app browser sandbox is still failing in this environment.
- Appendix remains available in the page but is hidden from default executive PDF/Word export.
