# Known Issues

- Browser print headers and footers can still show localhost URL, timestamp, and page numbers if enabled in the browser print dialog.
- CSS can hide app UI controls, but cannot reliably disable browser-controlled print headers/footers.
- Sprint 9.8A is layered on top of the current uncommitted Sprint 9.5 work.

